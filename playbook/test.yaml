#
# K8sコンポーネント配置に必要なディレクトリを作成
#
#
# 証明書をbootnodeのNFSサーバー上に準備
#
- name: Internal HA-PROXY install
  hosts:
  - mlbs
  vars:
    lb_pos: "Internal"
  gather_facts: true
  become: true
  become_user: root
  become_method: sudo
  roles:
    - haproxy
    - keepalived


