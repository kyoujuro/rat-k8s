apiVersion: v1
kind: Service
metadata:
  name: rook-ceph-mgr-dashboard
  namespace: rook-ceph
spec:
  ports:
  - name: https-dashboard
    port: 8443
    protocol: TCP
    targetPort: 8443
    nodePort: 30443
  selector:
    app: rook-ceph-mgr
    rook_cluster: rook-ceph
  type: NodePort
