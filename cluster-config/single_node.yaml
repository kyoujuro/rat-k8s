#
# 最小構成のK8sクラスタ
#

domain: example.com

### host network interfaice
bridge_if: "en0: Wi-Fi (Wireless)"
#bridge_if: "eno1"

### Kubernetes version
kubernetes_version: 1.18.2
kubernetes_custom: false

### K8s cluster/pod network CNI plugin
pod_network: bridge

### Cluster Network
private_ip_subnet: 172.16.2.0/24
public_ip_subnet:  192.168.1.0/24

## マスターか内部LBのIPをセットする
kube_apiserver_vip: 172.16.2.11


### Virtual Machine Spec
vm_spec:
  - name: "master1"
    cpu: 2
    memory: 2048
    box: "ubuntu/bionic64"
    private_ip:  "172.16.2.11"
    public_ip:   "192.168.1.211"
    pod_network: "10.244.1.0/24"    
    storage:
    playbook: "setup_linux.yml"
    comment: "Master node #1"
    


    

